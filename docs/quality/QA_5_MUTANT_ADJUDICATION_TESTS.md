# Document status: CURRENT

# QA-5 — Mutant adjudication and canonicalization boundary tests

## Objective

Adjudicate every undetected mutant reported by the bounded QA-4 PIT pilot and add only the tests that establish a real behavioral guarantee for `GenesisManifestVerifierCore`.

QA-5 does not chase a mutation percentage. It distinguishes test gaps from compiler-generated bytecode noise, synthetic accessors, unreachable generic branches, and equivalent behavior.

## Fixed baseline

```text
BASE_MAIN=81d3dbe96380b8372bf77210507d2496e800a202
BRANCH=qa/qa-5-mutant-adjudication-tests
PR_MODE=DRAFT
MERGE_AUTHORIZED=FALSE
```

## QA-4 evidence carried forward

```text
MUTANTS_GENERATED=63
KILLED=40
SURVIVED=12
NO_COVERAGE=11
UNDETECTED_TOTAL=23
MUTATION_SCORE=63.492063%
TEST_STRENGTH=76.923077%
```

The 23 undetected mutants were reviewed against the exact QA-4 XML report, the production verifier, and the existing verifier tests.

## Adjudication result

| Classification | Count | QA-5 action |
|---|---:|---|
| Behaviorally distinguishable test gap | 3 | Add normal API-level tests |
| Compiler null-check equivalent candidate | 9 | Record; do not create artificial tests |
| Semantically redundant string branch | 1 | Record for separate refactor decision |
| Synthetic private accessor outside active call graph | 4 | Record; do not test private compiler artifacts |
| Unreachable generic canonicalizer branch in current verify path | 5 | Record for separate removal/extraction decision |
| Unreachable null branch in current verify path | 1 | Record for separate removal decision |

This classification is evidence-based but remains subject to the repeated PIT execution on the exact QA-5 head.

## Behavioral gaps addressed

QA-5 adds three JVM tests through the normal `GenesisManifestVerifierCore.verify()` entry point:

1. a valid one-character path and kind, exercising the exact `StringBuilder(value.length + 2)` capacity boundary;
2. a `U+0020 SPACE` canonicalized value, proving that the control-character boundary is strictly below `0x20`;
3. a `U+001F UNIT SEPARATOR` canonicalized value, proving mandatory lowercase `\\u001f` escaping.

The tests derive the expected canonical record bytes independently and bind the approved core hash to those bytes. They do not invoke private methods through reflection.

## Explicit PIT test boundary

The QA-4 init script originally targeted only:

```text
com.morimil.app.data.genesis.GenesisManifestVerifierCoreTest
```

The first QA-5 candidate head proved that adding a separate JVM test class was insufficient: the Gradle unit-test task passed, but PIT did not examine the new class and the mutation inventory remained unchanged.

QA-5 therefore changes only the explicit `--targetTests` value to include both closed test classes:

```text
com.morimil.app.data.genesis.GenesisManifestVerifierCoreTest
com.morimil.app.data.genesis.GenesisManifestCanonicalizationBoundaryTest
```

No wildcard is used. The mutable production-class boundary remains exactly `GenesisManifestVerifierCore*`.

## Scope

Authorized repository changes are limited to:

```text
app/src/test/java/com/morimil/app/data/genesis/GenesisManifestCanonicalizationBoundaryTest.kt
tools/quality/android-pitest-pilot.init.gradle
docs/quality/QA_5_MUTANT_ADJUDICATION_TESTS.md
docs/quality/evidence/QA_5_MUTANT_ADJUDICATION_EVIDENCE_2026-08-06.md
```

QA-5 does not modify:

- application production source;
- the public verifier API;
- the approved Genesis core hash or approved file count;
- bundled Genesis assets or manifest contents;
- Room schemas, DAOs, identity, memory, writer, or continuity authority;
- GitHub workflow files;
- dependencies, release signing, or packaging;
- Body, Guardian, Seed, Genesis state, activation, or birth state.

## Required verification

The exact final PR head must demonstrate:

```text
JVM_BASELINE_TESTS=PASS
QA5_BOUNDARY_TESTS=3/3_PASS
PIT_TARGET_CLASS_PREFIX=UNCHANGED
PIT_TARGET_TESTS=EXACT_TWO_CLASS_SET
PIT_SOURCE_ATTRIBUTION_SCOPE=UNCHANGED
THREE_BEHAVIORAL_MUTANTS=KILLED
QUALITY_TOOL_TESTS=PASS
ANDROID_CI=PASS
REFERENCE_CHECKS=PASS
CODEQL=PASS
SBOM=PASS
GENESIS_BODY_PREPARATION_VALIDATION=PASS
```

The workflow named `Genesis Body Preparation` is an Android validation pipeline. Its success does not authorize or execute Genesis.

## Non-goals

```text
GLOBAL_MUTATION_THRESHOLD=NONE
REPOSITORY_WIDE_MUTATION_BASELINE=NOT_ESTABLISHED
PRODUCTION_REFACTOR=NOT_AUTHORIZED
RELEASE=NOT_AUTHORIZED
MERGE=NOT_AUTHORIZED
OPERATIONAL_BIRTH=NOT_OCCURRED
```

## Completion rule

QA-5 is technically complete only when:

1. the three new tests pass on the exact head;
2. repeated PIT proves the three targeted mutations are killed;
3. the remaining undetected mutants are preserved with explicit adjudication rather than hidden;
4. mutation class, test and source-attribution boundaries remain fail-closed;
5. the downloaded CI artifact is independently checked against its published digest and raw report;
6. all required workflows pass on the same final head;
7. the PR remains unmerged pending separate explicit authorization.
